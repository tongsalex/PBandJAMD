import React from 'react';
import '../normalize.css';
import './Header.css';

const Header = props => (
  <div id="header-container">
    <p id="siteTitle">PB & JAMD Forum</p>
  </div>
    );

export default Header;
